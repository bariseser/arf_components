pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        input(message: 'version', ok: 'tamam', submitter: 'Submitter', submitterParameter: 'SubmitterParameter')
      }
    }

  }
}